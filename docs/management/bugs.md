No.,指摘箇所,指摘内容,修正方針,状態
1,新規イベント作成,シリアルナンバー帯がイベント単位になっている。,QRごとにシリアルナンバーの採番方法を指定できるよう変更。,[ ]
2,新規イベント作成,出演アーティストが選べない（コネクション前提）。,未接続アーティストの選択、または出演交渉機能の追加。,[x]　済
3,イベント作成後画面,画面遷移先がなく行き止まりになっている。,これらの画面だけでなく、すべての画面にダッシュボードや１つ手前の画面等への戻り導線を追加。,[x]
4,イベント作成後画面,サンクス特典がイベントに紐づいている。,QRコード単位の紐づけに変更。,[x]
5,イベント作成後画面,承認が必要なエージェントが誰か不明。,担当エージェント名を表示。,[x]
6,QR作成画面,各プランの価格レンジが定義と不一致。,商品プランの定義をdbに持たせて、それに合わせること。スタンダードは3000円まで、メッセージは5000円まで、エントランスは30000円まで、カスタムは１００，０００円まで,[ ]
7,QR作成画面,価格指定がレンジのみ。,レンジ内でのワンプライス指定か、レンジ指定かを選択可能にする。デフォルトはワンプライス,[ ]
8,QR作成画面,配分対象にオーガナイザー自身を選べない。,属性に関わらず、出演者と主催者を選択可能にする。,[x] 済
9,QR作成画面,画面遷移先がなく行き止まりになっている。,ダッシュボードまたはイベント詳細への戻り導線を追加。,[x]
10,FOLLOWER INSIGHTS,エージェント権限でも「admin」と表示される。,ロール表示自体を不要として削除。,[ ]
11,FOLLOWER INSIGHTS,総フォロワーと各属性が並列表示。,総フォロワーのみをメインとし、内訳はランキングの母数として表示。,[ ]
12,新規イベント作成,Safari(Mac)で日時の入力フォームの高さがズレる。,CSSを調整し、ブラウザ間の表示を統一。,[ ]
13,イベント作成後画面,イベント内容（DJ追加等）の変更ができない。,承認後でも内容変更が可能なロジックに変更。,[x]
14,イベント作成後画面,作成済みのQRコードが編集・削除できない。,QRクリックで内容変更・削除を可能にする。,[X]
15,イベント作成後画面,QRコードの発行（表示・印刷）ができない。,QRクリックでコード表示、印刷、URL確認を可能にする。,[x] 済
16,ダッシュボード,一般ユーザー画面に「クリエイター申請」がある。,招待制のため、一般画面からは非表示。,[ ]
17,QR遷移先ページ,404エラーでページが表示されない。,遷移先ページのルーティングおよび生成処理の修正。,[x] 済
18,Stripe決済後,transactionsテーブルのcheck constraintエラー。,DB制約違反の解消（ステータス値の不整合修正）。,[x] 済
19,QR遷移先ページ,遷移先で商品タイプを再度選ばせている。,QR登録時に設定した商品タイプを自動適用すること。,[x]
20,QR遷移先ページ,メッセージ入力中に離脱が発生する。,決済を最優先。メッセージ入力は「決済後」に移動。メッセージの送信を受け取ってdbに格納,[ ]
21,決済後画面,フォローボタンが機能していない（数も不変）。,フォロー/解除状態の反映およびダッシュボード連携。,[ ]
22,決済後画面,ウォレット保存時にDomain Invalidエラー。,RP IDの設定修正（localhost環境等の考慮）。,[ ]
23,決済後画面,ログインへの導線がない。,ログインしてコレクションを確認できる導線を追加。,[ ]
24,オーガナイザーDB,決済成功しても残高が0のまま。,決済完了イベントと残高反映ロジックの紐づけ。,[x] 済
25,ユーザーDB,決済成功してもカードが表示されない。,購入済みコレクションとしての表示ロジック実装。,[x] 済
26,PW再設定メール,リンク先がトップページで再設定できない。,適切なパスワード再設定専用画面へ誘導。,[ ]
27,あらゆる送信メールの文面,英文一行のみでサービス感が乏しい。,日本のサービスらしい丁寧な定型文面に刷新。,[ ]
28,アーティスト画面,受取口座（Stripe）登録ボタンがない。,Stripeオンボーディング→審査→運営審査のフローを実装。,[x] 済
29,イベント管理の各イベント詳細画面,承認済みのイベントの修正ができない。,承認済みでも追加承認不要で修正可能とすること。出演者などしょっちゅう変更が入る。承認行為はイベントとして計画の実態があることをエージェントが承認するもの。一方、イベントの中止はエージェントの承認を必要とする。
30,イベント管理の各イベント詳細画面,出演者がわからない。,出演者一覧として配分可能なアーティストを一覧表示すること
31,エージェントダッシュボード,承認依頼がきていることがわからない,イベントの開催承認待ちのものがあったら、ダッシュボードの一番上にメッセージ出して承認待ち一覧のリンクを出すこと
32,オーガナイザーダッシュボード,承認依頼が承認されたことがわからない,承認依頼を出していたイベントが承認されたら、承認されたことを通知すること。
33,イベント詳細画面,出演交渉中のアーティストが表示されない。,ステータス付きで承認済みと交渉中は表示。辞退は非表示とする。
34,QR作成画面,出演交渉中のアーティストがいる場合、配分設定のプルダウンにunknownと表示される。出演承諾しててもunknownだった。,ちゃんと名前出すこと。出演交渉中の場合はプルダウンに出さない。ここは承認済みの人のみ。
35,イベント作成画面,他イベントで出演交渉中の人がいる場合、新規の別イベントを作ろうとするとその人が交渉中で表示されてしまう。,出演交渉はイベント単位。イベントが違えば初期状態にすること。
36,アーティストダッシュボード,出演依頼がパーティ名と会場しかわからない,出演依頼をクリックするとイベントの詳細がみれるようにすること。誰からの依頼なのかもわかるように
37,アーティストダッシュボード,出演依頼が、承認したら消えた。,アーティストダッシュボードに出演予定は常に表示。そのうえで交渉履歴として詳細画面が開き、辞退済みのオファーもみれるように。
38,イベント作成画面,出演依頼メッセージが送れない,出演交渉は条件やメッセージを送れるように。交渉機能だからね。
39,QR作成画面,配分設定の+追加ボタンを複数人の配分先で100％になっている状態で押すと、QR作成されてしまう。,配分先追加の枠が表示されるだけにすること
40,QR作成画面,エントランスタイプの価格が指定できない。,エントランスタイプ商品は必ず価格指定。レンジは設定できる価格のレンジであり、客が決められる額の範囲ではない。
41,QR作成画面,一番下の配分合計の意味がない。固定表示になっている,一番下の配分合計にアーティスト配分のパーセンテージを配分指定で指定した割合で表示すること。
42,イベント作成画面,出演者の選択で誰が誰なのかわからない,出演予定者のプルダウンの名前や、新規交渉予定のアーティストの名前をクリックすると、そのアーティストの詳細がみれるようにする。アーティストは似た名前が多い。誰なのかちゃんとわかるようにしないとオファーできない。
43,QR作成画面,カスタムが勝手に使えるようになっている,カスタムは基本的には利用不可。カスタム商品内容をエージェントと企画し、その企画が承認されて初めて使える。カスタムタイプの検討をエージェントに依頼するフォームとしておく
44,QR作成画面,エントランスタイプのタイプABがいつまでも買えてしまう。,前売りのタイプABは販売期間指定を必須にすること
45,QR作成画面,エントランスタイプのタイプBが誰でも使えてしまう。中止時の手数料が発生するタイプBは、販売予定額と販売枚数から求められる手数料額分の残高がないと利用できないようにすること
46,プロファイル編集画面,各ロールで入力項目が一緒になっている,ユーザー＜アーティスト＜オーガナイザー＜エージェントの順に必要項目を増やすこと。アーティスト以上の口座登録が必要な人はオンボーディングで聞かれる内容を全部登録必要とする。（Connect OAuthのリファレンス参照のことhttps://docs.stripe.com/connect/oauth-reference）さらに、アーティストは所属団体、クレジット表記、出演演目（ジャンルなど）、紹介文。オーガナイザーは加えて活動団体名、エージェントは今のところオーガナイザーと同じでいい。
47,QR作成画面,QRによる決済が誰宛てなのかわからない。,QRによる決済が誰宛てなのか選択を必須とする。決済の基本。,[x] 済
48,イベント詳細,アーティストダッシュボードから出演予定のイベントをクリックしてイベント詳細に飛ぶと、アーティストが勝手にQR作成できてしまう。,QR作成はオーガナイザーか担当エージェントのみ。アーティストができるのは、自分宛のQRのアフターフォローのみ（サンクスメッセージとか特典配布とか）
49,出金管理画面,現在売り上げている金額が保留中に計上されていない,計上すること。
50,アーティスト、オーガナイザーのダッシュボード,自分が送ったチアと受け取った金額がならんでいるのはわかりにくい,チアセクションと受け取った金額セクションはわけること
51,全般,ソース上にstripeの決済手数料とシステム利用料がオンコーディングされている。,DBによる定義参照にして利率変動が定義箇所の変更1つで全部変わるようにすること。
52,QR作成画面,宛先の指定が配分にない人も指定できてしまう。,配分で指定されている人のなかから選ぶように変更。画面の配置も配分指定が先で、主となる受け取り人はその下で指定するようにする。
53,QR変更画面,QRの内容変更してもしなくても、変更確定すると配分の内容が同じ内容でどんどん増えていく。,無条件で配分先をinsertで書き込んでるのではないか？insertとupdateをちゃんと区別して更新かけること。,[x]
54,QR変更画面,QRを削除しても消えない。,論理削除し、画面から消すこと。,[x]
55,イベント編集画面,なんの変更もしてなくても、再承認が必要になってしまう。,どこが更新されたか判断して再承認になるようにしろ。承認済みのイベントの日程と場所が変わった場合は最承認が必要なながれにすること。という要件が無視されている。,[x]
56,イベント作成画面,開始日が入力されてるのに終了日は開始日起点のカレンダーになってない。,開始日が入力されたら、終了日は開始日起点のカレンダーから開始。イベント変更画面も同様であれば修正。,[x]
57,QR作成画面と変更画面,QRコードで販売する画像のアップロード機能がない。,保管はSupabase Storageを使う。react-easy-cropを使い、1:1の正方形で直感的にクロップできるUI。通信負荷を減らすため、アップロード前にクライアント側でリサイズ。インスタのように、操作がサクサク進むUXを優先してコードを書いてくれ,[x]
58,QRから遷移した支払いページ,商品タイプを選ぶものは不要。,商品タイプはけす。,[x]
59,イベントページ,出演アーティストの名前がハイフンになっている。,表示項目名をアーティストの名前としてだすこと。,[x]
60,ダッシュボード画面,cheersを送ってゲットしたカードを眺めるコレクション画面へのリンクがないし、コレクション画面自体存在していない。,コレクションへのリンクとその画面を作ること,[x]
61,プロファイル編集画面,ロールがオーガナイザーなのに、プロファイル入力がファンの項目になっている。,ロールごとの入力内容のフォームに切り替えること。,[x]
62,プロファイル編集画面,口座登録に必要な情報を全部いれても、口座登録ボタンがアクティブにならない。,必要な項目の判定がおかしいのでチェックして直すこと。,[x]
63,プロファイル編集画面,口座登録ボタンを押したら、通信エラーとでる。,調査のうえ修正せよ。,[x]
64,adminロール,口座登録承認機能がない。,承認依頼がきてたら通知がでるように。そして承認画面を新規で作ること。,[x]
65,イベント詳細画面,出演予定アーティスト名がハイフン,名前を取得して表示すること,[x]
66,アーティストダッシュボード,出演依頼が届いていて、承諾したが、出演予定イベントに出ない。,承諾したイベントはQRコードの有無に関わらず表示すること。,[x]
67,QR作成画面,配分先の名称がハイフン表示になっている。,名前を取得して表示すること。,[x]
68,QR再編集画面、作成画面も,画像アップロードした結果のプレビューがない。アップロードした画像がcheersカードに合成されない。,欠落している機能を追加すること,[x]
69,アーティスト出演予定からのイベント詳細画面,自分が受け取り配分に含まれているQRの情報はみれるようにすること。あなたの着金予定（配分 30%）と表示されているのもなんの情報だかわからない。表示必要かな？,指摘事項の通り対応ねがう。,[x]
70,アーティストプロファイル編集画面,郵便番号検索が動かない。かならず検索失敗する。,うごくようにすること。,[x]
71,アーティストプロファイル編集画面,口座登録ボタン押したら"This field may contain only katakana, spaces, dashes and dots."と出て進まない。,原因調査のうえすすめられるように。,[x]
72,adminの審査待ち画面,審査対象者をクリックしたらそいつのプロフィールと誰が紹介者かわかる画面に遷移して確認する審査情報がほしい。,指摘の画面追加とそのリンクを追加。,[x]
73,口座開設申請却下後,却下になると担当エージェントにお尋ねくださいとなるだけで、エージェントが誰かもわからないし、再申請もできない。,担当エージェントはプロフィール画面で必ず示すようにすること。あと、再申請可能にすること。,[x]
74,イベント詳細画面,イベント開催の証跡アップロードのやりかたがわからない。,アップロードしたうえで、承認依頼を出すフローを実装すること,[x]
75,エビデンス提出画面,イベント終了後にエビデンスを提出できますとなっていて、終了を認識していない様子。,おそらく日時チェックが入っている。終了してたら日時はどうでもいい。,[x]
76,adminダッシュボード,エビデンス提出の承認依頼が来ているのに通知されていない。，通知は必須、さらに、精算管理メニューに入っても対象申請が表示されない。,[x]
77,イベント管理画面,証跡提出後、追加提出というボタンになるが、押してもダッシュボードに飛ばされるだけ。,承認前なら差し替え可能でよい。,[x]
78,バッチ処理,stripe照合機能を作る。,パーティ終了となった翌日朝5時に、前日までの決済でstripe照合が済んでいないものがあったら、stripeに問い合わせる電文作って投げるバッチを作る。投げたあと、stripeからの戻りを待ち、戻り内容に入っているstripeの計算結果でdbの値を更新するようにする。このとき、stripe照合済みのフラグをたてる。最後にイベント単位に未照合が残っているかチェックし、全部完了していたら、イベントの照合済みフラグを立てる。あわせてadminのダッシュボードにこの照合機能の状況照会画面へのリンクをつける。その画面には何日分のバッチが処理対象件数何件中、照合ok何件、照合して書き換えたの何件、未照合何件がわかるようになってること。,[x]
79,オーガナイザーダッシュボード,通知が消えるタイミングが不明,エビデンス差戻しの通知はその差戻し状態が再提出とかで進んだらきえていいし、イベント承認のお知らせも、お知らせふんだあとイベント詳細ページに飛んだ時点で消していい。,[x]
80,出勤管理画面,出勤管理画面がチープ,保留中や出勤可能な額の内訳としてイベント単位の承認状況、照合状況、payout可能日をみせれるようにすること。,[x]
81,照合管理画面,イベント別照合状況で、照合待ち件数が残っているイベントは、その場で照合処理を起動できるようにすること。,そのイベント分の未照合明細を問い合わせるバッチを起動する。,[x]
82,イベント作成と承認のフロー,イベント承認フローが認識と違う,イベント作成したら即エージェントに承認依頼が飛ぶのではなく、オーガナイザーがこれで承認依頼という任意のタイミングで依頼できるようにすること。また、承認前であってもQRコード作成は可能とする。QRが有効にならないだけにする。
83,QRの有効期限,QRコードの有効期限を管理すること。,エントランスタイプのA,B以外はパーティ当日まで決済不可、パーティ終了後3時間で決済不可になるようにコントロールする。エントランスタイプのA,Bは決めた販売期間の間だけ有効になるようにする。設定はQR作成画面でできるようにする。なおテスト用にはバイパスできるよう、スイッチをつけておくこと。QR照会画面には有効期間を必ず表示すること。,[x]
84,QR作成画面,色々あるので列挙する。,エントランスタイプはワンプライスのみ、レンジはない。金額蘭は、先頭ゼロサプレス、3桁ごとにカンマ入るように。テスト用有効期間バイパスのスイッチがバグってる。オフでオンの位置。オンにするとさらに右にずれてスイッチの外にうごく。,[x]
85,QR作成画面,エントランスタイプのQRを作ろうとするとnew row violates row-level security policy for table "products"って出る。,どうなっているか調査のうえ対処して,[x]
86,アーティストダッシュボード,出演予定と出演イベントは情報が同じ,どちらか１つでよい。
87,アーティスト出金管理,ダッシュボードやイベント詳細見ても精算額は1555円なのに、出金管理画面では1729円て出る。,この違いは何？
88,cheersカード,決済後のcheers!カード表示時にAppleウォレット格納が動いていない。,動くようにしてくれ。書き出した.p12ファイル（と、設定したパスワード）、Pass Type ID、Team IDはわかるのできいてくれ。
89,QR後の決済前画面,シングルプライスなのに横スライドバーはいらない。非活性で置いとくのもなし。,消すこと。
90,決済後cheers画面, Early Supporter！最初の10人に入りましたの次の画像が？になっている。,直せ
91,決済後cheers画面,パスキーでウォレット登録ボタンをおしたらこのメッセージ「The RP ID "localhost" is invalid for this domain」,まずウォレット登録とパスキー登録と、出すボタンを整理したうえで対応すすめる。
92,決済後cheers画面から、add to apple walletボタン押したら,{"error":"Not found"}とでるだけ。うごかない。,動くようにしてくれ。
93,決済後のcheers画面,cheersカードの宛先がQRの宛先じゃなくて主催者固定になっている。ハートマークの隣にARTISTと入っているが、これはどこから？宛先名が入るようにしてくれ。決済したメールアドレスは重要情報、どのアドレスにcheersカードがついたのか明示すること。決済に使用したメアドですでに会員かどうかわかるので、アカウント作成かログインしてカードを確認かどちらかだけ表示するようにすること。apple wallet格納ボタンがわかりにくい。
94,wallet内容,デザインをちょっと変えたい,ロゴマークのところはピンクのハートアイコンでいい。いまDirect Cheersって出しているところに、cheerの宛先をだしてくれ。artistの表記はいらないな。
95,カードコレクション画面,サンクスメッセージが届いていることが見れない。,決済後画面のように、サンクスメッセージの通知と裏面表示ができるようにすること。
96,イベント詳細,決済の通知改善,メッセージタイプのメッセージが見れるように、掲示板がほしい。掲示板に決済の時刻金額を出すのと、メッセージタイプはメッセージ内容を出す。どっちもリアルタイムで出したい。そして、その決済金額が、チャリーンってカウントされるようにしてほしい。
97,アーティストのイベント詳細,決済ログの表示について,決済ログは自分が配分に含まれているものだけ見えるようにすること。金額はお客さんの投入した金額しか見えてないが、そのうちいくら受け取るのかを表示し、この金額の方が目立つようにすること。
98,イベント詳細,メッセージの送信先,決済ログで見せるメッセージ内容は、宛先の人だけが読めるようにすること。決済ログはどこまで見せるつもりか？タブをわけてみてはどうか。
99,招待機能,どのメアドで招待されているかわからない,招待対象メアドを表示すること。すでに会員かどうかわかってるんだから、会員ならログインして受け取るだし、会員じゃないなら新規登録して受け取る。だよね。自分でわかるんだから2つ出さない。招待出す時は、メアドを任意にしているけど必須にすること。
100,招待機能,オーガナイザーからDJを一人招待したが、サインアップ時にメアド確認が飛んで、メールのリンクを踏んでも404がでる。,サインアップできるように修正。
101,各種メール,簡素な英文ではなく、日本のサービスが出すちゃんとした文面にするよう以前指示したはずだが、サインアップのメアド確認メールが変わっていなかった。,出すメール文面全てチェックして見直せ
102,profile編集画面,アバターurl指定してもアバター画像がページヘッダのユーザーアイコンに反映されない。,せめて画面遷移したタイミングで再描写してほしい。
103,ログイン全般,パスキーによるログインに対応していない。,パスキーでもログインできるようにすること。
104,プロフィール編集画面,アバター画像はURL指定じゃなくて、画像アップロードにしたい。,supabaseのstrageに格納しておくこと。
