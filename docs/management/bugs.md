No.,指摘箇所,指摘内容,修正方針,状態
1,新規イベント作成,シリアルナンバー帯がイベント単位になっている。,QRごとにシリアルナンバーの採番方法を指定できるよう変更。,[ ]
2,新規イベント作成,出演アーティストが選べない（コネクション前提）。,未接続アーティストの選択、または出演交渉機能の追加。,[ ]
3,イベント作成後画面,画面遷移先がなく行き止まりになっている。,ダッシュボード等への戻り導線を追加。,[ ]
4,イベント作成後画面,サンクス特典がイベントに紐づいている。,QRコード単位の紐づけに変更。,[ ]
5,イベント作成後画面,承認が必要なエージェントが誰か不明。,担当エージェント名を表示。,[ ]
6,QR作成画面,各プランの価格レンジが定義と不一致。,商品プランの定義（マスター）に合わせる。,[ ]
7,QR作成画面,価格指定がレンジのみ。,レンジ内でのワンプライス指定か、レンジ指定かを選択可能にする。,[ ]
8,QR作成画面,配分対象にオーガナイザー自身を選べない。,属性に関わらず、出演者と主催者を選択可能にする。,[x] 済
9,QR作成画面,画面遷移先がなく行き止まりになっている。,ダッシュボードまたはイベント詳細への戻り導線を追加。,[ ]
10,FOLLOWER INSIGHTS,エージェント権限でも「admin」と表示される。,ロール表示自体を不要として削除。,[ ]
11,FOLLOWER INSIGHTS,総フォロワーと各属性が並列表示。,総フォロワーのみをメインとし、内訳はランキングの母数として表示。,[ ]
12,新規イベント作成,Safari(Mac)で日時の入力フォームの高さがズレる。,CSSを調整し、ブラウザ間の表示を統一。,[ ]
13,イベント作成後画面,イベント内容（DJ追加等）の変更ができない。,承認後でも内容変更が可能なロジックに変更。,[ ]
14,イベント作成後画面,作成済みのQRコードが編集・削除できない。,QRクリックで内容変更・削除を可能にする。,[ ]
15,イベント作成後画面,QRコードの発行（表示・印刷）ができない。,QRクリックでコード表示、印刷、URL確認を可能にする。,[x] 済
16,ダッシュボード,一般ユーザー画面に「クリエイター申請」がある。,招待制のため、一般画面からは非表示。,[ ]
17,QR遷移先ページ,404エラーでページが表示されない。,遷移先ページのルーティングおよび生成処理の修正。,[x] 済
18,Stripe決済後,transactionsテーブルのcheck constraintエラー。,DB制約違反の解消（ステータス値の不整合修正）。,[x] 済
19,QR遷移先ページ,遷移先で商品タイプを再度選ばせている。,QR登録時に設定した商品タイプを自動適用。,[ ]
20,QR遷移先ページ,メッセージ入力中に離脱が発生する。,決済を最優先。メッセージ入力は「決済後」に移動。,[ ]
21,決済後画面,フォローボタンが機能していない（数も不変）。,フォロー/解除状態の反映およびダッシュボード連携。,[ ]
22,決済後画面,ウォレット保存時にDomain Invalidエラー。,RP IDの設定修正（localhost環境等の考慮）。,[ ]
23,決済後画面,ログインへの導線がない。,ログインしてコレクションを確認できる導線を追加。,[ ]
24,オーガナイザーDB,決済成功しても残高が0のまま。,決済完了イベントと残高反映ロジックの紐づけ。,[x] 済
25,ユーザーDB,決済成功してもカードが表示されない。,購入済みコレクションとしての表示ロジック実装。,[x] 済
26,PW再設定メール,リンク先がトップページで再設定できない。,適切なパスワード再設定専用画面へ誘導。,[ ]
27,メール文面,英文一行のみでサービス感が乏しい。,日本のサービスらしい丁寧な定型文面に刷新。,[ ]
28,アーティスト画面,受取口座（Stripe）登録ボタンがない。,Stripeオンボーディング→審査→運営審査のフローを実装。,[x] 済